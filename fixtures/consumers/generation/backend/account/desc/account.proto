// @nexa $contract: "nexa.dev/source-comment/v1"
syntax = "proto3";

package account.v1;

option go_package = "example.com/nexa-generation-consumer/backend/account/internal/pb;accountpb";

message GetAccountRequest {
  string id = 1;
}

message GetAccountResponse {
  string name = 1;
}

service AccountService {
  // @nexa auth: "none"
  // @nexa http.method: "GET"
  // @nexa http.path: "/accounts/{id}"
  rpc Get(GetAccountRequest) returns (GetAccountResponse);
}
